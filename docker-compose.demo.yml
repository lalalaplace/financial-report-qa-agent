services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: financial_demo
      POSTGRES_USER: demo_user
      POSTGRES_PASSWORD: demo_password
    ports:
      - "5432:5432"
    volumes:
      - ./demo/init_demo_db.sql:/docker-entrypoint-initdb.d/init_demo_db.sql:ro
